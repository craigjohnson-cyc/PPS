@* @using DataModels.Models.PickList

@{
    var respPersons = ViewData["choices"] as IEnumerable<ResponsiblePerson> ?? new List<ResponsiblePerson>();
    if (typeof(System.Collections.IEnumerable).IsAssignableFrom(ViewData.ModelMetadata.ModelType) && ViewData.ModelMetadata.ModelType != typeof(string))
    {
        @Html.ListBox("", respPersons, Model.ResponsiblePerson)
    }
    else
    {
        @Html.DropDownList("", respPersons)
    }
} *@